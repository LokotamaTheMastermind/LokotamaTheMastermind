import type { Config } from "tailwindcss";

export default {
  content: [],
  darkMode: "class",
  theme: {
    extend: {
      fontFamily: {
        sans: "Poppins, sans-serif",
        serif: "Playfair Display, serif",
      },
    },
  },
  plugins: [],
} satisfies Config;
